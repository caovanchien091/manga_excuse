import 'package:common/common.dart';
import 'package:flutter/material.dart';

class SourceRoute extends RouteRegister {
  @override
  List<String> routes = [];

  @override
  Route? register(settings, injection) {
    switch (settings.name) {
    }
  }
}
